[project]
name = "quantv2"
version = "0.1.0"
description = "Event-driven equity quant research system"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
